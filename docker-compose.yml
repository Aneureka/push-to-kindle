version: "3"

volumes:
  uploads:

services:
  web:
    build: .
    ports:
      - "8001:8001"
    volumes:
      - uploads:/tmp/push-to-kindle/
    restart: unless-stopped
    image: aneureka/push-to-kindle